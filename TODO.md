# TODO

Работаем с linux-машины с niri. Дев-машина — macOS (кросс-компиляция чистая,
CGO запрещён). Архитектурный контекст — README.md.

## M0 — переезд и фундамент ✅ (2026-08-29, сделано на macOS)

- [x] новая папка `lapsus`, git init
- [x] перенесены `layout` / `dict` / `analyze` / `config` из oshitkeyb,
      включая 2 коммита из сломанного worktree `impl-continue`
- [x] таблица layout домержена из wksw: сдвиговый ряд (`@ # $ ^ &`),
      `? ~ < > " :`, `` ` ``↔`ё`; конфликт `'\'`→`я` убран (таблица wksw,
      проверенная на реальной машине)
- [x] реальные частотные словари top-20k (FrequencyWords/OpenSubtitles 2018)
      вместо сломанного EN-словаря из системного словаря
- [x] libei выкинут (niri не даёт EIS — тупик), CGO нет
- [x] `lapsus check`, `lapsus convert` (авто-направление)
- [ ] старые репо wksw/oshitkeyb: пометить deprecated / заархивировать,
      оставить указатель на lapsus (сделать на linux-машине, там git remote)

## M1 — `lapsus fix`: one-shot фикс последнего слова по хоткею ✅ ядро (2026-08-29, сделано на linux-машине с niri 26.04)

Триггер — бинд в niri KDL: `Mod+Shift+S { spawn "lapsus" "fix"; }`

- [x] подкоманда `fix` (+ `-v` лог, `-n` dry-run, flock от двойного нажатия)
- [x] niri IPC через `NIRI_SOCKET` (`niri msg --json focused-window`):
      детект терминала по app_id — точное сравнение (не подстрока: «st»
      ловил бы «ghostty»), регистронезависимое
- [x] GUI-режим: `wtype` Ctrl+Shift+Left (выделить слово) →
      `wl-paste --primary` (с ретраями) → `analyze` → `wtype -- <слово>`.
      **Проверено вживую: waterfox, адресная строка, ghbdtn → привет,
      свитч на Russian; скриншот-пруф в сессии от 2026-08-29**
- [x] терминал-режим: wksw-путь (выделение мышью заранее → primary) был
      заменён на честную замену: стирание BackSpace × rune-длина слова +
      ввод (см. `wayland.ReplaceWord`). **Проверено вживую: Alacritty
      (app_id "Alacritty"), 2026-08-29 — путь без буфера обмена,
      clipboard больше не затирается.** Оговорка: стирание бьёт по слову
      слева от каретки — чинить слово, набранное только что (это и есть
      основной кейс); со старым выделением в другом месте строки — нет
- [x] переключение раскладки после фикса: `niri msg action switch-layout`
      принимает только `next|prev|<index>` (имя — НЕЛЬЗЯ); точный индекс
      берётся из `niri msg --json keyboard-layouts` (names + current_idx,
      появился в свежих niri; на старых свитч молча пропускается);
      таргет = раскладка исправленного слова, если она уже активна — не трогаем.
      Живое подтверждение niri#3568: после wtype-событий раскладка
      соседних окон иногда слетает — порядок «инъекция → свитч» обязателен
- [x] **niri#3568**: порядок «инъекция → switch-layout» зашит в пайплайн
      и зафиксирован юнит-тестом (TestFixGUIHappyPath)
- [x] конфиг-ключи M1: `hotkey.terminals`, `fix.switch_layout`, `fix.pause_ms`
- [x] антихрупкость: flock; ретраи чтения primary (пусто → пауза → ещё);
      отказ заменять мультистрочное/длинное (>64 рун) выделение;
      выделение схлопывается (Right) при «фикс не нужен» и dry-run
- [x] ручной чек-лист: ~~waterfox~~ ✅, ~~Alacritty~~ ✅, ~~Zed~~ ✅
      (в т.ч. выделение фразы, 2026-08-29); осталось проверить руками:
      Obsidian, Dolphin (KDE-поля), GTK-диалоги, electron-приложения
- [x] хоткеи M1 (2026-08-29): правый Alt → слово у каретки,
      правый Alt+Ctrl → выделенное (`fix --selection`). Правый Alt
      превращён в Compose (compose:ralt): клавишу-модификатор соло в niri
      забиндить нельзя — матчинг требует строгого равенства модификаторов,
      а на нажатии самого модификатора он уже зажат. Два хоткея вместо
      угадывания «есть ли живое выделение»: primary selection переживает
      своё окно и подставляется в чужое (поймано вживую на себе)
- [x] обновить README: бинд, схема, конфиг, отладка

## M2 — `lapsus daemon`: авто-режим ✅ ядро (2026-08-29, сделано на linux-машине)

- [x] чтение `/dev/input/eventX` **без grab** (группа `input`, без root) —
      чистый stdlib вместо `holoplot/go-evdev`: discovery по
      /sys/class/input/*/capabilities/key (hex-битмап, бит KEY_A+KEY_Q+SPACE),
      чтение сырых 24-байтных input_event; hotplug — рескан каждые 3 с
- [x] буфер текущего слова + граница: буквы/цифры копятся (классификация
      ПО СИМВОЛУ, не по коду клавиши — иначе «может» ломается на «ж»=
      KEY_SEMICOLON); фикс-границы space/, . ; / ? ! :; Enter/Tab/стрелки/
      Ctrl-Alt-комбо/прочее — сброс буфера без фикса; пауза
      `daemon.boundary_pause_ms` (300) с generation-счётчиком против
      протухших таймеров
- [x] замена унифицирована с терминальным путём M1: BackSpace × rune-длина
      слова + ввод исправления (`wayland.ReplaceWord`), каретка гарантированно
      после слова, раскладка — после инъекции (niri#3568);
      раскладка и фокус трекаются через `niri msg --json event-stream`
      (JSON-стримы, KeyboardLayoutsChanged приходит при подключении)
- [x] пер-апп исключения: `daemon.exclude_app_ids`, кейс-инсенситив;
      тумблер паузы — SIGUSR1 (`pkill -USR1 lapsus`, бинд в niri);
      индикатор состояния (statusbar) — не сделано
- [x] systemd user unit: deploy/lapsus.service
- [x] границы слов (2026-08-29, по находке из быта): слово завершается
      ЛЮБЫМ печатным разделителем (пробел, пунктуация, кавычки, скобки,
      дефис — «слово)» чинится как «слово »); Enter/Tab/стрелки/комбо —
      сброс без фикса. Паузная граница по умолчанию ВЫКЛЮЧЕНА
      (boundary_pause_ms=0): история — дефолт 300 мс разрезал «ltkf» на
      «lt»+«kf» и обрывок «kf» чуть не починился в «ла»; 1000 мс всё ещё
      резал при наборе с раздумьем. Финальное решение — разделители-only,
      пауза — opt-in в [50,5000] + daemon.min_word_len=3 от обрывков и
      одиночных букв (хоткей M1 без ограничений). Вторая находка из быта
      (2026-08-29): «rfr » → «rкак» — разделитель (пробел) стоит МЕЖДУ
      словом и кареткой и съедал один BackSpace; фикс: стирать
      «слово+разделитель», печатать «исправление+разделитель» (знак
      сохраняется, но переворачивается по позиции вместе со словом
      (третья находка из быта: «руддщ,» → «hello,» вместо «hello?» —
      знак набран теми же клавишами той же неверной раскладки; теперь
      «руддщ,» → «hello?», «ghbdtn,» → «приветб» — чистый Punto-стиль).
      Четвёртая находка (2026-08-30): авто-переключение раскладки демоном
      раздражает — он не знает, на каком языке ты продолжишь; теперь
      демон (daemon.switch_layout=false) чинит только слово, а раскладку
      двигают явные хоткеи (fix.switch_layout=true): Alt = слово+раскладка вместе
- [ ] бытовой прогон: задержка, мультиплексоры (tmux), игры.
      Правки слова: одиночный Backspace ужимает буфер; Delete/стрелки/
      Ctrl-комбо — сброс без фикса; автоповтор (зажатые клавиши)
      инвалидирует буфер — длине нельзя доверять, фикс отключается до
      следующего слова (2026-08-29, иначе possible wrong-length deletion);
      клик мышью демону невидим — слово до клика не чинится
- [ ] тумблер по double-tap (нативно в niri нельзя — см. переписку 2026-08-29;
      вариант — evdev-детект в самом демоне)

## M3 — (опция, только если M2 бесит) честный IME

`input-method-v2` (niri поддерживает, fcitx5 работает) — поток клавиш и
surrounding text без selection-хаков и слипов. Минусы: только Rust (в Go
нет живых wayland-биндингов), работает лишь в text-input приложениях,
конфликтует с fcitx5/ibus. Не браться, пока M2 не испробован.

## Известные грабли

- **niri#3568** — события виртуальной клавиатуры сбрасывают xkb group:
  порядок всегда «inject → switch-layout».
- `niri msg action switch-layout` принимает только `next|prev|<index>`
  (имя раскладки — нет); текущий индекс — из `niri msg --json
  keyboard-layouts` (нет в старых niri). С `track-layout "window"`
  свитч действует на раскладку сфокусированного окна.
- primary selection обновляют не все приложения (многие терминалы — нет),
  отсюда отдельный терминал-режим.
- `wtype` шлёт keysyms — вставляет кириллицу независимо от активной
  раскладки (это наш козырь, не баг).
- частоты en/ru из одного корпуса (OpenSubtitles) — сопоставимы между
  собой; если менять источник для одного языка, порог `×2` в analyze
  надо калибровать заново.
